# 待確認位置

座標可疑、但自動比對無法定案的場站。確認後把座標寫進 `data/coord-overrides.json`
（key＝`縣市|場站名`，或直接用**場站 id** 當 key；id key 優先。同名場站一定要用 id，
否則一個名稱 key 會同時套到兩筆），並從本檔移除。

## 判定方式

三個獨立來源互比，落差大＝可疑：

| 來源 | 定位對象 | 典型失效 |
| --- | --- | --- |
| Nominatim / OSM | 地址門牌（`geocode-carmochi.mjs`，結果存 `data/geocode-cache.json`） | 門牌不存在就退成整條路的中心；「對面／旁」失真；路名撞名跨區 |
| NLSC 國土測繪中心 | 政府門牌坐標（`geocode-fallback.mjs`） | 門牌本身對，但停車場不在門牌上 |
| Autopass POI | **停車場實體**（`api.autopass.xyz/v3/pois`，`scrape-autopass.mjs`） | 冷門場站未收錄；同名多筆 |

Autopass 定位的是停車場本身而非地址，可信度最高，優先採用。
品牌來源自帶官方座標者（嘟嘟房地圖連結內嵌的 X/Y、台灣聯通、ViVi PARK）以官方值為準，
不要用 override 壓過去——官方後來補上座標時，記得把當初的補校 override 移掉。

---

## 目前待確認：無（2026-08-17 清空）

當次處理紀錄：

- 對全部 595 筆「座標由地址反查」的場站做 NLSC 門牌交叉比對，依落差分成
  A 類（>1km，59 筆）、B 類（500m–1km，78 筆）、C 類（200–500m，129 筆），
  另加 D 類（地址無門牌號、結構上無法反查，57 筆）。
- 全部比對 Autopass POI 後：**Autopass 修正 293 筆＋人工確認 13 筆，合計 306 筆座標校正**。
- 同名候選座標相近者（緯經度小數第 2 位相同，多為同場站的不同出入口、大小車分區或
  計費方案後綴）依「取地址最接近者、地址相同則任取」自動定案 11 筆。
- 同名 2 筆的場站改用 id 當 key 分開釘死：臺中榮總（`p1a4c549` 本院南院區／
  `piy7kwq` 北二院區）、國美館（`p14amkhk` 民生園區／`p1x9yy29` 美術園區）、
  天祥站（`p71cuqp` 24TPS／`p14e7194` 嘟嘟房）。
- 嘟嘟房「桃園益壽站」「龍潭中正站」重打官網 API 確認已下架，隨爬蟲更新移除
  （資料集 1508 → 1506 筆）。「華山站」原缺座標曾用 Autopass 補，官網現已提供官方
  座標（相距 58m），故移除該 override 改用官方值。

全站現在 0 筆無座標、0 筆地址待確認。

---

## 後續（尚未處理）

- 有 76 筆現行座標與 NLSC 完全相同（0m），因為本來就是走 NLSC fallback 定出來的，
  NLSC 對這批是循環驗證、不算獨立佐證。這批未納入本次比對，需要時可用 Autopass 或
  OSM Overpass（`amenity=parking` 實體幾何）另外查。
- 另有約 900 筆 utg／dodohome／vivipark 自帶座標的場站，未做過任何交叉驗證。
- `web/parking/near/淡水老街.html` 因「千斗五股水碓平面」歸位到五股（原本錯放淡水 11km）
  後，1.5km 內場站不足 `POI_MIN = 4` 而不再產生。要保留該頁需放寬門檻或換地標。
